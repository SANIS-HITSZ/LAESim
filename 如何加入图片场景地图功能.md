# 如何加入图片场景地图功能

这份文档记录 LAESim 第一版“图片地图平面”功能。目标是支持纯视觉 VIO + 2D 地图匹配定位仿真平台：输入一张地图图片和物理比例尺，把它变成 UE 里的可碰撞平面，并允许载具按地图坐标出生。

## 1. 功能边界

当前版本做的是“平面地图”，不是完整 GIS / 地形系统：

- 支持 png / jpg / bmp 等 UE 可导入图片
- 支持任意长宽比图片
- 支持按 `meters_per_pixel` 缩放到真实物理尺寸
- 支持设置地图中心、Z 高度、Yaw
- 支持启用碰撞，让车、船等载具站在地图上
- 支持设置分割 ID，方便视觉任务过滤地图平面
- 支持像素坐标和 AirSim NED 世界坐标互相转换
- 支持按 GPS / 经纬度指定载具初始位置

暂不支持：

- 高度图地形
- 自动识别道路 / 建筑 / 障碍物
- 多张地图拼接
- UE 内置 UI 上传按钮

后续如果做 UI，建议直接复用本功能的 `simLoadSceneMap` API，而不是新建一套加载逻辑。

## 2. settings.json 启动加载

顶层增加 `SceneMap`：

```json
"SceneMap": {
  "Enabled": true,
  "ImagePath": "C:/path/to/demo_map.png",
  "ObjectName": "LAESimSceneMap",
  "MetersPerPixel": 0.05,
  "PixelCoordinateFrame": "NorthUp",
  "CenterX": 0,
  "CenterY": 0,
  "Z": 0,
  "Yaw": 0,
  "CollisionEnabled": true,
  "SegmentationId": 21,
  "GeoReference": {
    "Enabled": true,
    "ReferenceLatitude": 22.591244,
    "ReferenceLongitude": 113.969778,
    "ReferenceAltitude": 21,
    "ReferenceU": 1024,
    "ReferenceV": 450
  }
}
```

实现位置：

- `AirLib/include/common/AirSimSettings.hpp`
- `Unreal/Plugins/AirSim/Source/SimMode/SimModeBase.cpp`
- `Unreal/Plugins/AirSim/Source/WorldSimApi.cpp`

`SimModeBase::BeginPlay()` 会在生成载具前加载地图。这样地图碰撞体先存在，车和船出生时可以直接落在地图平面上。

`PixelCoordinateFrame` 有两种常用值：

- `NED`：兼容旧版本，像素 `U` 增大对应 AirSim `+X`，像素 `V` 增大对应 AirSim `+Y`
- `NorthUp`：适合 Google Earth / 卫星图截图，并配合 UE 显示层 -90 度轴向补偿。像素坐标仍按图像直观输入；GPS 出生会把 AirSim NED 的南向位移映射到地图局部 `+Y`，从而对应像素 `U` 增大。

使用 `NorthUp` 时，源码会自动给 UE 里的地图平面做 -90 度视觉轴向补偿，并且 GPS / 像素 / ROS 坐标转换会同步使用这套补偿后的轴向。用户不需要再手动把 `Yaw` 加 90 度。

`GeoReference` 用一个已知 GPS 点给图片配准。通常做法是在 Google Earth 或其他地图工具中导出一张干净卫星图，然后选一个明显点，记录它的经纬度和它在图片中的像素位置 `ReferenceU / ReferenceV`。如果使用图片中心作为参考点，`ReferenceU / ReferenceV` 就分别取图片宽高的一半。

### 2.1 图片和参数准备

导入图片前至少准备这些输入：

- `ImagePath`：图片在 Windows 主机上的绝对路径。UE 运行在 Windows，所以不要写 WSL 的 `/home/...` 路径。
- `MetersPerPixel`：每个像素代表多少米。这是 2D 地图匹配最关键的比例尺参数。
- `PixelCoordinateFrame`：卫星图 / Google Earth 截图推荐写 `NorthUp`；旧版普通测试图可写 `NED`。
- `GeoReference`：如果要用 GPS 出生，就必须提供一个参考点的经纬度和它在图片上的像素位置。

图片宽高不需要手写。LAESim 会在加载图片时直接读取 `Texture2D` 的 `GetSizeX()` / `GetSizeY()`，然后用 `图片宽高 * MetersPerPixel` 得到 UE 平面尺寸。

图片建议：

- 推荐使用已经裁掉 Google logo、比例尺、按钮、图例等 UI 的干净卫星图。
- 支持任意长宽比，不要求正方形。
- 当前版本只把整张图变成一个可碰撞平面，不识别道路、建筑和障碍物。

`GeoReference` 字段说明：

- `ReferenceLatitude / ReferenceLongitude`：参考点的纬度 / 经度，GPS 出生时最重要。
- `ReferenceU / ReferenceV`：同一个参考点在图片里的像素坐标，左上角为 `(0, 0)`。如果参考点就是图片中心，写图片宽高的一半。
- `ReferenceAltitude`：参考点海拔。对于 2D 平面地图匹配不影响水平位置，可以写 Google Earth 看到的海拔，也可以固定写一个合理值，例如 `0` 或场地平均海拔。

## 3. 载具按地图出生

旧的 `X/Y/Z/Yaw` 方式仍然可用。新增方式是在单个载具里写 `StartOnSceneMap`。

这两类方式的区别：

- 旧方式 `X/Y/Z/Yaw`：直接给 AirSim NED 世界坐标，和图片像素、GPS 配准没有关系。
- 新方式 `StartOnSceneMap`：先按地图像素、地图局部米制坐标或 GPS 计算出生点，再写回载具的 AirSim NED 坐标。只要 `Enabled` 没关，它会覆盖同一载具里的旧 `X/Y/Z/Yaw`。

像素模式：

```json
"StartOnSceneMap": {
  "CoordinateType": "Pixel",
  "U": 800,
  "V": 600,
  "Height": 5,
  "Yaw": 0
}
```

地图局部米制坐标模式：

```json
"StartOnSceneMap": {
  "CoordinateType": "Meters",
  "MapX": 10,
  "MapY": -5,
  "Height": 0,
  "Yaw": 90
}
```

GPS / 经纬度模式：

```json
"StartOnSceneMap": {
  "CoordinateType": "GPS",
  "Latitude": 22.591300,
  "Longitude": 113.969900,
  "Altitude": 21,
  "Height": 5,
  "Yaw": 90
}
```

坐标约定：

- 图片左上角是像素原点
- `PixelCoordinateFrame = NorthUp` 时，像素模式保持 `U` 沿图面右方、`V` 沿图面下方；GPS 模式会把纬度减小产生的南向位移映射到图面右方，也就是 `U` 增大
- `PixelCoordinateFrame = NED` 时，保持旧版 U->X、V->Y 约定
- `Height` 表示离地图平面的高度，最终 AirSim `Z = SceneMap.Z - Height`
- `Height` 只控制载具离地图平面多高，不影响二维地图上的 `U/V` 或经纬度水平位置。无人机常写 `5`、`10`，车和船通常写 `0`。
- `Altitude` 是 GPS 模式里的载具海拔。当前 2D 地图出生主要使用经纬度计算水平位置，`Altitude` 不决定图面上的像素位置；如果不写，会使用 `ReferenceAltitude`。
- `StartOnSceneMap.Yaw` 会叠加到 `SceneMap.Yaw`
- `GPS` 模式需要 `SceneMap.GeoReference.Enabled = true` 才能和卫星图严格对齐；否则会退回 AirSim `OriginGeopoint`

示例文件：

```text
how_to_use_settings/settings_scene_map_1uav_1car_1boat.json
```

## 4. Python API

这些 API 挂在 `VehicleClient` 上，默认走 `41451` CV / 世界级端口：

```python
client = airsim.VehicleClient(port=airsim.RPCLIB_PORT_CV)
client.confirmConnection()

client.simLoadSceneMap(
    "C:/path/to/demo_map.png",
    0.05,
    center_x=0,
    center_y=0,
    z=0,
    yaw=0,
    collision_enabled=True,
    object_name="LAESimSceneMap",
)

info = client.simGetSceneMapInfo()
world = client.simSceneMapToWorld(800, 600, 0)
pixel = client.simWorldToSceneMap(world.x_val, world.y_val)
client.simUnloadSceneMap()
```

Windows 侧工具：

```powershell
python .\Multi_use\scene_map_tools.py info
python .\Multi_use\scene_map_tools.py load "$env:USERPROFILE\Documents\AirSim\maps\demo_map.png" --meters-per-pixel 0.05
```

## 5. ROS 接口

ROS 侧同样走世界级 RPC 客户端，端口来自 `ApiServerPortCV`，默认 `41451`。

话题：

```text
/airsim_node/scene_map/info
```

服务：

```text
/airsim_node/scene_map/load
/airsim_node/scene_map/unload
/airsim_node/scene_map/get_info
/airsim_node/scene_map/scene_map_to_world
/airsim_node/scene_map/world_to_scene_map
```

消息和服务定义位置：

```text
ros/src/airsim_ros_pkgs/msg/SceneMapInfo.msg
ros/src/airsim_ros_pkgs/srv/LoadSceneMap.srv
ros/src/airsim_ros_pkgs/srv/UnloadSceneMap.srv
ros/src/airsim_ros_pkgs/srv/GetSceneMapInfo.srv
ros/src/airsim_ros_pkgs/srv/SceneMapToWorld.srv
ros/src/airsim_ros_pkgs/srv/WorldToSceneMap.srv
```

调用示例：

```bash
rosservice call /airsim_node/scene_map/get_info
rosservice call /airsim_node/scene_map/scene_map_to_world "{u: 800, v: 600, z: 0}"
rosservice call /airsim_node/scene_map/world_to_scene_map "{x: 10, y: 0}"
```

运行时切换地图：

```bash
rosservice call /airsim_node/scene_map/load "{
image_path: 'C:/path/to/demo_map.png',
meters_per_pixel: 0.05,
center_x: 0,
center_y: 0,
z: 0,
yaw: 0,
collision_enabled: true,
object_name: 'LAESimSceneMap'
}"
```

## 6. UE 实现细节

`WorldSimApi::simLoadSceneMap()` 做了这些事：

1. 用 `FImageUtils::ImportFileAsTexture2D` 从磁盘加载图片。
2. 使用 UE 内置 `/Engine/BasicShapes/Plane.Plane` 作为平面网格。
3. 使用 AirSim 已有动态材质，把图片贴到 `TextureParameter`。
4. 根据 `图片像素 * meters_per_pixel` 设置 StaticMeshComponent 缩放。
5. 根据 `CollisionEnabled` 设置碰撞开关。
6. 把 Actor 注册到 `scene_object_map`，方便分割 ID、对象查询等 AirSim 逻辑使用。

注意：这里生成的是平面，不是高度场。它解决“贴图地图 + 可碰撞承载面”的初版需求。

## 7. 加功能时踩过的坑

- `RpcLibAdaptorsBase::SceneMapInfo` 里不要把字段命名成 `z`。`MSGPACK_DEFINE_ARRAY(...)` 展开时会和 msgpack 内部 `zone` 名称冲突，曾经导致 `msgpack_object is not a member of ... zone` 编译错误。当前适配层用 `z_val`，Python 再兼容映射成 `.z`。
- `simLoadSceneMap()` 不要在游戏线程 lambda 里再调用另一个等待游戏线程的 `simUnloadSceneMap()`，否则有死锁风险。当前做法是在同一个游戏线程任务里直接清理旧 Actor。
- `applySceneMapVehicleStartSettings()` 需要是 public。`SimModeBase.cpp` 要在加载地图后、生成载具前调用它。
- `NorthUp` 地图的 -90 度视觉补偿要在 `SpawnActor` 时直接传入 Actor 初始旋转。不要先用零旋转生成 Actor、注册 `StaticMeshComponent`，再调用 `SetActorLocationAndRotation` 去转 Static 组件；这种顺序可能出现日志显示 `VisualYaw` 已变，但 UE 里地图贴图方向没有实际变化。
- `NorthUp` 的 GPS 出生点不能直接用 `local_x=north, local_y=east`。当前 UE 显示补偿下，GPS 需要用 `local_x=east, local_y=-north`。曾经只改了地图视觉旋转和像素 API，没有改 GPS 到地图局部坐标这一步，导致同经度、纬度更小的 UAV 位置一直不变。
- 图片路径必须是 UE 所在 Windows 主机能访问的绝对路径。WSL 里的 `/home/...` 路径不能直接给 Windows UE 用。
- 如果开启碰撞后载具出生点和地图高度不一致，优先检查 `SceneMap.Z`、载具 `Height` 和 UE 关卡里是否还有其他地面挡住。

## 8. 编译验证

每次改完这个功能相关代码，至少跑：

```powershell
python -m py_compile PythonClient\airsim\client.py PythonClient\airsim\types.py Multi_use\scene_map_tools.py
cmd /c BuildAirSimRelease.bat
```

如果要放进外部 UE 项目，还需要把插件同步到项目的 `Plugins/AirSim`，再跑对应 UE 项目的 `UnrealBuildTool` 或直接打开 UE 触发编译。
