# 如何加入新的载具类型

本文记录这次在 `LAESim` 中加入 `Boat` 载具的完整路径。参照旧基线工程 `E:\AAA_project\CETC\AirSim_Multi-main` 对比，完成一个新载具不能只做到 UE 里能生成模型，而是要让 C++ 核心、UE Pawn、Python API、ROS 接口、settings 模板、示例脚本、端口和传感器都闭环。

## 1. 完成标准

一个新载具类型至少要满足这些条件：

- `settings.json` 可以写入多个该类型实例，并且可以和无人机、车同时存在。
- AirLib 能识别新的 `VehicleType`，能创建对应 API，能在多端口 API Server 中分流。
- 载具有自己的控制量、状态量、RPC client/server/adaptor。
- Unreal 插件有对应 `Pawn` 和 `PawnSimApi`，并接入 `SimModeAirGround` 或目标 SimMode。
- PythonClient 有对应 `Client`、`Controls`、`State` 和示例脚本。
- ROS 有对应 msg、publisher、subscriber、wrapper 调用和示例脚本。
- 相机、GPS、IMU、lidar 等传感器能通过 settings 配置，行为至少和车/无人机同等级可用。
- 有独立通信端口，且 Windows API 与 ROS 都能连接。
- 默认模型资源随插件一起分发，并由 Pawn 代码直接加载；普通用户不需要在 `settings.json` 中指定模型路径。
- 能通过 AirLib、UE 插件、Python 语法、ROS catkin 的编译或基础检查。

## 2. 这次相对基线工程做了什么

和 `AirSim_Multi-main` 相比，这次主要新增或修改了这些部分：

- AirLib 核心：新增 `AirLib/include/vehicles/boat/*`、`AirLib/src/vehicles/boat/api/*`，并修改 `AirSimSettings.hpp`、`Common.hpp`、`MultiPortApiServer.hpp`、`AirLib.vcxproj`。
- UE 插件：新增 `Unreal/Plugins/AirSim/Source/Vehicles/Boat/BoatPawn.*`、`BoatPawnSimApi.*`，修改 `Vehicles/AirGround/SimModeAirGround.*`，并把 AirLib 里的 boat 头文件/源文件同步到插件 `Source/AirLib`。
- Python：修改 `PythonClient/airsim/client.py`、`PythonClient/airsim/types.py`，加入 `BoatClient`、`BoatControls`、`BoatState`、`RPCLIB_PORT_BOAT`。
- ROS：新增 `BoatControls.msg`、`BoatState.msg`，修改 `airsim_ros_wrapper.*` 和 `airsim_ros_pkgs/CMakeLists.txt`，加入 `ros/src/example/keyboard_boat_ros.py`。
- 示例与 settings：新增 `Multi_use/boat_keyboard_control.py`、`how_to_use_settings/settings_airground_2uav_1car_1boat_with_sensors.json`，并更新相关 README。
- 模型资源：导入 `E:\AAA_project\CETC\boat\model\Type_052B_Destroyer.obj/.mtl/textures`，生成 `Type_052B_Destroyer_Combined.uasset`，源码仓库保存到 `Unreal/Assets/Boat/Models/Boat`，构建时由 `build.cmd` 复制到插件内容目录 `Unreal/Plugins/AirSim/Content/Models/Boat`。

## 3. AirLib 核心层

先从 `AirLib` 做类型定义和 RPC 闭环：

1. 在 `AirLib/include/common/AirSimSettings.hpp` 里加入新枚举和字符串识别，例如 `VehicleType::Boat`，并让 `SimpleBoat` / `PhysXBoat` 映射到该类型。
2. 在 `AirLib/include/common/Common.hpp` 里加入新端口常量。这次使用：
   - `ApiServerPort` / CV：`41451`
   - `ApiServerPortCar`：`41461`
   - `ApiServerPortUAV`：`41471`
   - `ApiServerPortBoat`：`41481`
3. 在 `AirLib/include/api/MultiPortApiServer.hpp` 里增加 Boat server，确保 `VehicleType::Boat` 走船端口。
4. 新增 `BoatApiBase.hpp` 定义控制和状态接口，例如 `setBoatControls`、`getBoatState`。
5. 新增 `BoatRpcLibAdaptors.hpp`、`BoatRpcLibClient.hpp/.cpp`、`BoatRpcLibServer.hpp/.cpp`，把 `BoatControls`、`BoatState` 注册到 rpclib。
6. 新增 `BoatApiFactory.hpp` 和 `simple_boat/SimpleBoatApi.hpp`。`SimpleBoatApi` 要实现 `VehicleApiBase` 的纯虚函数，至少包括 `resetImplementation`、`update`、`getStatusMessages`、`enableApiControl`、`isApiControlEnabled`、`armDisarm`、`canArm`、`getHomeGeoPoint`、`getLandedState`、`getKinematicsEstimated`、`getEnvironment` 等。
7. 把新增 `.cpp` 加入 `AirLib/AirLib.vcxproj` 和 `cmake/AirLib/CMakeLists.txt`。ROS 链接 AirLib 时依赖 CMake 列表，漏掉会出现 `BoatRpcLibClient` 未定义一类错误。

船的动力学在 `SimpleBoatApi` 中做简化平面三自由度模型：

- `u`：纵向速度，对应船体前后方向。
- `v`：横向漂移速度，模拟转弯侧滑。
- `r`：艏向角速度，模拟船体转向惯性。
- 控制量用 throttle / rudder / brake，状态量包含 `speed`、`forward_speed`、`lateral_speed`、`yaw_rate`。

这套模型不做波浪、水流、浮力、横摇、纵摇。仿真时船仍在地面平面运动，可以把地面区域涂成蓝色表示水域。

## 4. Unreal 插件层

UE 层负责让载具真的出生、显示、更新姿态并绑定传感器。

1. 新增 `Unreal/Plugins/AirSim/Source/Vehicles/Boat/BoatPawn.h/.cpp`。这里创建船体 mesh、根组件、摄像机挂点和 fallback 外观。
2. 新增 `BoatPawnSimApi.h/.cpp`。它负责把 AirLib 中的姿态、碰撞、传感器和 Unreal pawn 连接起来。
3. 修改 `Unreal/Plugins/AirSim/Source/Vehicles/AirGround/SimModeAirGround.h/.cpp`：
   - include `BoatPawn.h`、`BoatPawnSimApi.h`、`BoatApiFactory.hpp`。
   - 遍历 `settings.json` 的 Vehicles 时识别 Boat。
   - Spawn `ABoatPawn`。
   - 给 Boat 创建 `PawnSimApi` 和 API provider。
4. 如果把插件复制到 UE 项目，例如 `D:\Epic\Proiect\Multi\Plugins\AirSim`，要同步 `Source/Vehicles/Boat`、`Source/AirLib/include/vehicles/boat`、`Source/AirLib/src/vehicles/boat` 和构建后生成的 `Content/Models/Boat` 内容资源。

这次遇到的 UE 编译坑：

- `SimpleBoatApi` 没实现 `VehicleApiBase` 的所有纯虚函数时，UE 编译会报抽象类不能实例化。
- UE 4.27 的 `SpawnActor` 重载要写成 `world->SpawnActor<ABoatPawn>(spawn_position, spawn_rotation, params)` 这一类形式。混用 `StaticClass()`、指针位置参数的旧写法会导致编译失败。
- UE 弹窗只显示 `Multi could not be compiled. Try rebuilding from source manually.` 时，真正错误在 `D:\Epic\Proiect\Multi\Saved\Logs\Multi.log` 或 UBT 输出里，要看 C++ 首个 error，而不是只看弹窗。

## 5. Python API 与示例

Python 侧要和 car / multirotor 一样完整：

1. 在 `PythonClient/airsim/types.py` 中新增：
   - `BoatControls`
   - `BoatState`
2. 在 `PythonClient/airsim/client.py` 中新增：
   - `RPCLIB_PORT_BOAT = 41481`
   - `BoatClient`
   - `setBoatControls`
   - `getBoatState`
   - `MultiVehicleClient` 中按 vehicle type 或名字走 Boat client 的逻辑
3. 加入可直接跑的示例，例如 `Multi_use/boat_keyboard_control.py`：
   - 支持 `--vehicle Boat`
   - 支持 `--host`
   - 使用 throttle / rudder / brake 控制
   - 打印 `speed`、`forward_speed`、`lateral_speed`、`yaw_rate`

基础检查：

```powershell
python -m py_compile E:\AAA_project\CETC\LAESim\PythonClient\airsim\client.py E:\AAA_project\CETC\LAESim\PythonClient\airsim\types.py E:\AAA_project\CETC\LAESim\Multi_use\boat_keyboard_control.py
```

## 6. ROS 接口

ROS 侧至少要让控制和状态都能像车一样走 topic。

1. 在 `ros/src/airsim_ros_pkgs/msg` 增加：
   - `BoatControls.msg`
   - `BoatState.msg`
2. 修改 `ros/src/airsim_ros_pkgs/CMakeLists.txt`，把两个 msg 加入 `add_message_files`。
3. 修改 `ros/src/airsim_ros_pkgs/include/airsim_ros_wrapper.h`：
   - 增加 boat publisher/subscriber 数据结构。
   - 增加 Boat client 管理。
4. 修改 `ros/src/airsim_ros_pkgs/src/airsim_ros_wrapper.cpp`：
   - 为每艘船发布 `/airsim_node/<BoatName>/boat_state`。
   - 订阅 `/airsim_node/<BoatName>/boat_cmd`。
   - 调用 `BoatRpcLibClient::setBoatControls` 和 `getBoatState`。
5. 增加示例 `ros/src/example/keyboard_boat_ros.py`，并更新 `_ros_example_common.py`、`vehicle_state_monitor_ros.py`。

常用 topic：

```text
/airsim_node/Boat/boat_cmd
/airsim_node/Boat/boat_state
/airsim_node/Boat/front_center_scene/Scene
/airsim_node/Boat/lidar/Lidar
/airsim_node/Boat/gps/Gps
/airsim_node/Boat/imu/Imu
```

ROS 编译检查：

```bash
cd /mnt/e/AAA_project/CETC/LAESim/ros
catkin_make
```

如果环境里指定了不存在的 `g++-8`，先去掉该指定或换成本机已有编译器。

## 7. settings 写法

`settings.json` 顶层端口要包含 Boat：

```json
{
  "SettingsVersion": 1.2,
  "SimMode": "AirGround",
  "ApiServerPort": 41451,
  "ApiServerPortCar": 41461,
  "ApiServerPortUAV": 41471,
  "ApiServerPortBoat": 41481
}
```

单艘船示例：

```json
"Boat": {
  "VehicleType": "SimpleBoat",
  "X": 0,
  "Y": 40,
  "Z": 0,
  "Yaw": 90,
  "Sensors": {
    "imu": { "SensorType": 2, "Enabled": true },
    "gps": { "SensorType": 3, "Enabled": true },
    "magnetometer": { "SensorType": 4, "Enabled": false },
    "barometer": { "SensorType": 1, "Enabled": false },
    "lidar": {
      "SensorType": 6,
      "Enabled": true,
      "NumberOfChannels": 16,
      "Range": 80,
      "PointsPerSecond": 10000,
      "RotationsPerSecond": 10,
      "HorizontalFOVStart": -180,
      "HorizontalFOVEnd": 180,
      "VerticalFOVUpper": 10,
      "VerticalFOVLower": -20,
      "X": 0,
      "Y": 0,
      "Z": -1
    }
  },
  "Cameras": {
    "front_center_scene": {
      "X": 1.5,
      "Y": 0,
      "Z": -1.2,
      "Pitch": 0,
      "Roll": 0,
      "Yaw": 0,
      "CaptureSettings": [
        { "ImageType": 0, "Width": 1280, "Height": 720, "FOV_Degrees": 90 }
      ]
    }
  }
}
```

多艘船直接复制该结构并改实例名和出生点，例如 `Boat2`、`Boat3`。控制脚本通过 `--vehicle Boat2` 指定实例。

传感器建议直接仿照车：相机、GPS、IMU、lidar 可用；`magnetometer` 和 `barometer` 对地面平面载具建议显式关闭，避免被默认无人机传感器逻辑误注入。

## 8. 导入并设置默认模型

本工程的 Boat 模型采用和 AirSim 自带无人机、汽车相同的思路：`settings.json` 只声明载具实例和 `VehicleType`，不写模型资源路径。Boat 的默认外观由 `BoatPawn.cpp` 直接加载插件内置资源 `Type_052B_Destroyer_Combined`。只有要替换整个 Pawn 类时，才需要考虑 `PawnPath`；单纯换默认船模型不走 settings。

需要注意仓库目录和构建目录的区别：

- 源码仓库保留 Boat 资产源：`Unreal/Assets/Boat/Models/Boat`。
- `build.cmd` 构建时把上面的资源复制到 AirSim 插件挂载路径：`Unreal/Plugins/AirSim/Content/Models/Boat`。
- `Unreal/Plugins/AirSim/Content` 是构建/部署后的插件内容目录，不建议把整包内容作为 GitHub 源码上传；只上传 Boat 资产源和必要源码更干净。
- 运行时 `BoatPawn.cpp` 加载的仍是 `/AirSim/Models/Boat/...`，因为复制到插件 Content 后资产挂载名就是 `/AirSim`。

这次本地模型在：

```text
E:\AAA_project\CETC\boat\model
```

主要文件：

```text
Type_052B_Destroyer.obj
Type_052B_Destroyer.mtl
textures\*.jpg
```

因为原始 OBJ 里有很多 `o` / `g` 对象分组，UE 自动导入时会拆成很多小 StaticMesh。为了让 `BoatPawn` 默认加载一个稳定资源，先生成合并版 OBJ：

```powershell
$src = 'E:\AAA_project\CETC\boat\model\Type_052B_Destroyer.obj'
$dst = 'E:\AAA_project\CETC\boat\model\Type_052B_Destroyer_Combined.obj'
'o Type_052B_Destroyer_Combined' | Set-Content -LiteralPath $dst -Encoding ASCII
Get-Content -LiteralPath $src | Where-Object { $_ -notmatch '^(o|g) ' } | Add-Content -LiteralPath $dst -Encoding ASCII
```

导入到插件内容目录，目标路径是 `/AirSim/Models/Boat`。可以用 UE4.27 的 ImportAssets commandlet，例如创建 `import_boat_combined_asset.json`：

```json
{
  "ImportGroups": [
    {
      "FileNames": [
        "E:/AAA_project/CETC/boat/model/Type_052B_Destroyer_Combined.obj"
      ],
      "DestinationPath": "/AirSim/Models/Boat",
      "bReplaceExisting": true,
      "FactoryName": "FbxFactory",
      "ImportSettings": {
        "bImportMaterials": true,
        "bImportTextures": true,
        "bCombineMeshes": true
      }
    }
  ]
}
```

执行：

```powershell
& 'D:\Epic\UE\UE_4.27\Engine\Binaries\Win64\UE4Editor-Cmd.exe' `
  'D:\Epic\Proiect\Multi\Multi.uproject' `
  -run=ImportAssets `
  -importsettings='D:\Epic\Proiect\Multi\import_boat_combined_asset.json' `
  -unattended -nop4 -NoLogTimes
```

注意：

- commandlet 可能返回非 0，并打印 `Could not connect to source control!`。这次即使有这个错误，`.uasset` 仍然成功生成了，要检查 `Plugins\AirSim\Content\Models\Boat` 下是否出现目标资源。
- OBJ 可能出现 smoothing group、near-zero normal/tangent 警告，通常不影响编译和加载；如果视觉质量不够，再回建模软件里重算 normals / smoothing groups。
- 插件 `AirSim.uplugin` 必须有 `"CanContainContent": true`，否则内容资源不会按 `/AirSim/...` 被加载。

导入后，先把资源同步回源码资产目录：

```text
E:\AAA_project\CETC\LAESim\Unreal\Assets\Boat\Models\Boat
```

然后运行 `BuildAirSimRelease.bat` 或 `build.cmd --Release`。脚本会把它复制到：

```text
E:\AAA_project\CETC\LAESim\Unreal\Plugins\AirSim\Content\Models\Boat
```

如果再把插件部署到外部 UE 项目，例如 `D:\Epic\Proiect\Multi`，复制构建后的整个 `Unreal\Plugins\AirSim` 插件即可；这一步会带上 `Content\Models\Boat`。

在 `BoatPawn.cpp` 中默认加载。这个路径写死在 Pawn 构造函数里，所以用户新增 `Boat`、`Boat2`、`Boat3` 时不需要在 `settings.json` 写任何模型路径：

```cpp
static ConstructorHelpers::FObjectFinder<UStaticMesh> boat_mesh(
    TEXT("StaticMesh'/AirSim/Models/Boat/Type_052B_Destroyer_Combined.Type_052B_Destroyer_Combined'"));
```

这次模型原始长轴在本地 Z 轴，视觉上方对应本地 -Y 方向，不是 UE / AirSim Pawn 期望的 `X = 前向、Z = 上方`。因此在 `BoatPawn` 中对 mesh 做了旋转修正：本地 Z 对齐 Pawn 的 +X 前向，本地 -Y 对齐 UE 的 +Z 上方。

```cpp
hull_->SetRelativeRotation(FRotator(0.0f, -90.0f, 90.0f));
hull_->SetRelativeScale3D(FVector(10.0f, 10.0f, 10.0f));
```

同时给 `BoatPawn` 加了独立 `USceneComponent` 根组件，让模型缩放只影响船体，不把相机、传感器挂点一起放大。放大船体后要同步检查默认相机基座，否则相机会落进船体或被甲板遮挡。本工程把 052B 默认相机挂点外移和上移到：

```cpp
camera_front_center_base_->SetRelativeLocation(FVector(900, 0, 380));
camera_front_left_base_->SetRelativeLocation(FVector(850, -220, 360));
camera_front_right_base_->SetRelativeLocation(FVector(850, 220, 360));
camera_driver_base_->SetRelativeLocation(FVector(120, 0, 520));
camera_back_center_base_->SetRelativeLocation(FVector(-900, 0, 340));
```

原则是：船体 mesh 可以独立缩放，但相机基座要在 Pawn 根组件坐标系里重新放到船体外侧或上方；不要直接缩放根组件，否则会把相机、传感器和碰撞参考一起放大。正常部署时应保留 `Unreal/Assets/Boat/Models/Boat` 中的 `Type_052B_Destroyer_Combined.uasset` 及其材质、贴图资源，并让 `build.cmd` 复制到插件 `Content\Models\Boat`；如果资源缺失，`BoatPawn` 才会回退到 C++ 生成的简化舰船外观，方便定位是资源缺失还是 API / ROS 链路问题。

### 8.1 后续想更换 Boat 模型怎么办

要区分两件事：

- `settings.json` 不能直接填写 StaticMesh 路径来换模型。
- `settings.json` 的 `PawnPath` 可以替换 Pawn 类，也就是替换整套 Pawn 蓝图 / C++ 类。

如果只是把所有 Boat 的默认外观换成另一个模型，推荐两种方式：

1. 保持资源路径不变：把新模型导入并覆盖 `/AirSim/Models/Boat/Type_052B_Destroyer_Combined`，再把生成的 `.uasset` 同步到 `Unreal/Assets/Boat/Models/Boat`。这样 `BoatPawn.cpp` 不用改，所有 `SimpleBoat` 仍然加载同一个默认资源名，但实际外观变成新模型。
2. 修改代码路径：把新模型导入并同步到 `Unreal/Assets/Boat/Models/Boat` 后，修改 `BoatPawn.cpp` 里的 `FObjectFinder<UStaticMesh>`：

```cpp
static ConstructorHelpers::FObjectFinder<UStaticMesh> boat_mesh(
    TEXT("StaticMesh'/AirSim/Models/Boat/MyNewBoat.MyNewBoat'"));
```

如果需要某一艘船使用不同 Pawn，可以使用 AirSim 的 `PawnPaths` / `PawnPath`。示例：

```json
{
  "PawnPaths": {
    "MyBoatPawn": {
      "PawnBP": "Class'/Game/Blueprints/BP_MyBoatPawn.BP_MyBoatPawn_C'"
    }
  },
  "Vehicles": {
    "Boat2": {
      "VehicleType": "SimpleBoat",
      "PawnPath": "MyBoatPawn",
      "X": 0,
      "Y": 52,
      "Z": 0,
      "Yaw": 90
    }
  }
}
```

这里的 `PawnPath` 不是模型路径，而是 `PawnPaths` 中的 key；`PawnBP` 才是 UE 蓝图类路径。当前 `AirGround` 里 Boat 相关相机、碰撞事件会把 Pawn 转成 `ABoatPawn`，所以自定义蓝图应继承自 `ABoatPawn`。如果换成完全无关的 `APawn`，还要同步修改 `SimModeAirGround` 的类型判断、相机获取和初始化逻辑。

## 9. 编译与验证

AirLib 编译：

```powershell
cd E:\AAA_project\CETC\LAESim
cmd /c BuildAirSimRelease.bat
```

UE 项目插件编译：

```powershell
& 'D:\Epic\UE\UE_4.27\Engine\Binaries\DotNET\UnrealBuildTool.exe' `
  Development Win64 `
  -Project='D:\Epic\Proiect\Multi\Multi.uproject' `
  -TargetType=Editor `
  -Progress -NoEngineChanges -NoHotReloadFromIDE
```

Python 检查：

```powershell
python -m py_compile `
  E:\AAA_project\CETC\LAESim\PythonClient\airsim\client.py `
  E:\AAA_project\CETC\LAESim\PythonClient\airsim\types.py `
  E:\AAA_project\CETC\LAESim\Multi_use\boat_keyboard_control.py
```

settings JSON 检查：

```powershell
python -m json.tool C:\Users\32749\Documents\AirSim\settings.json > $null
```

ROS 检查：

```bash
cd /mnt/e/AAA_project/CETC/LAESim/ros
catkin_make
```

运行时检查：

- UE 启动 `AirGround`，确认 settings 中的 `Boat`、`Boat2`、`Boat3` 能出生。
- Windows 侧运行 `python E:\AAA_project\CETC\LAESim\Multi_use\boat_keyboard_control.py --vehicle Boat`。
- ROS 侧运行 `rosrun example keyboard_boat_ros.py --vehicle Boat`。
- 用 `rostopic list | grep Boat` 看 `boat_cmd`、`boat_state`、相机、GPS、IMU、lidar topic。
- 用状态监控脚本确认 `forward_speed`、`lateral_speed`、`yaw_rate` 会随 throttle / rudder 改变。

## 10. 常见问题

- UE 只弹 `Multi could not be compiled. Try rebuilding from source manually.`：去看 UBT 输出或 `Saved\Logs\Multi.log`，弹窗不是根因。
- 新 API 类抽象类报错：检查是否实现了 `VehicleApiBase` 新版本的所有纯虚函数。
- `SpawnActor` 编译不过：UE4.27 用正确重载，不要照旧代码混用 `StaticClass()` 和指针位姿参数。
- ROS 链接找不到 Boat RPC 符号：检查 `cmake/AirLib/CMakeLists.txt` 是否包含 `src/vehicles/boat/api/*.cpp`。
- Python 能 import 但连不上：检查 `ApiServerPortBoat` 和 `RPCLIB_PORT_BOAT` 是否一致，默认是 `41481`。
- settings 中有多艘船但脚本控制错实例：所有 API / ROS 示例都要显式传 `vehicle_name` 或 `--vehicle`。
- 模型不显示：检查 `.uasset` 是否在插件 `Content\Models\Boat` 下，检查 `FObjectFinder` 路径是否是 `/AirSim/Models/Boat/...`，并确认 `AirSim.uplugin` 允许内容资源。
- 模型方向或尺寸不对：先在 Pawn 内只旋转/缩放 mesh 组件，不要缩放根组件，否则传感器和相机会一起偏移。
- 编译后仍加载旧代码：删除对应 UE 项目的 `Binaries` / `Intermediate` 后重新 UBT 编译，但不要误删源工程目录。
- `Invoke-WebRequest` 报 EOF、基础连接关闭，或 `Expand-Archive` 报找不到中央目录结尾记录：这是依赖 zip 下载不完整。删除 `eigen3.zip`、`suv_download_tmp` 和残缺的 `AirLib\deps\eigen3` 后重跑 `BuildAirSimRelease.bat`；高模 SUV 下载失败会回退默认车，Eigen 下载失败才会导致 AirLib 编译失败。
